<% flash.each do |key, value| %>
  <div class="alert alert-<%= key %>">
    <a href="#" data-dismiss="alert" class="close" style="margin-top:5px">×</a>
      <h5><%= value %></h5>
  </div>
<% end %>
